apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    name: redis-service
    app: demo-voting-app
spec:
  selector:
    app: redis-app-pod
    name: demo-voting-app
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
