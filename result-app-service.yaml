apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    name: result-service
    app: demo-voting-app
spec:
  selector:
    app: result-app-pod
    name: demo-voting-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005
